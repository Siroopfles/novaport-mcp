# Project Settings
PROJECT_NAME="NovaPort MCP"

# Database URL
# The default and recommended configuration for local development.
# This creates a database file within a '.novaport_data' folder in your workspace.
# Leave this line unchanged, the application generates the correct URL per workspace.
DATABASE_URL="sqlite:///./dummy_for_alembic.db"

# Example for a production PostgreSQL database (requires adjustment of FTS migrations):
# DATABASE_URL="postgresql://conport_user:conport_password@localhost:5432/conport_db"